Create a prompt block that is easy to copy and paste usable for AI code generation. Do it in the chat box and don't create files.

Output ONLY a single fenced code block using ```text ... ```, no other text.