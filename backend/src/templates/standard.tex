\documentclass[11pt]{article}
\usepackage[margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{hyperref}
\usepackage{fontawesome5}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{fancyhdr}
\usepackage{parskip}
\usepackage{graphicx}
\usepackage{tikz}
\usepackage{needspace}

% Grayscale accent colors for visual hierarchy
\definecolor{sectiongray}{gray}{0.40}     % 40% black for section titles
\definecolor{companygray}{gray}{0.30}     % 30% black for companies/institutions
\definecolor{boldgray}{gray}{0.25}        % 25% black for bold titles (job titles, etc.)

% Clickable link styling: blue text (no underline)
\definecolor{linkblue}{HTML}{0563C1}
\hypersetup{
  colorlinks=true,
  urlcolor=linkblue,
  linkcolor=linkblue,
  citecolor=linkblue,
  breaklinks=true,
}

% Standard font settings - contemporary sans-serif
\renewcommand{\familydefault}{\sfdefault}

% Standard section styling - bold with thin black rule
\newcommand{\standardsection}[1]{%
  \noindent{\Large\textbf{\textcolor{sectiongray}{#1}}}\\[-1ex]%
  \rule{\textwidth}{0.3pt}\\[0.6ex]%
}

% Better spacing for itemize
\setlist[itemize]{topsep=0pt, itemsep=0pt, parsep=0pt, partopsep=0pt, leftmargin=*}
\pagenumbering{gobble}

% Standard paragraph spacing - generous contemporary spacing
\setlength{\parskip}{0.7\baselineskip}
\setlength{\parindent}{0pt}

% Prevent orphaned/widowed lines and page breaks in sections
\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000
\brokenpenalty=10000

% PDF compression for smaller file size
\pdfcompresslevel=9
\pdfobjcompresslevel=3

\begin{document}
\pagestyle{empty}

{PERSONAL_INFO_HEADER}

{CONTACT_INFO_SECTION}

{CONTENT_SECTIONS}

\end{document}
