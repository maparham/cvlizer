{
  "name": "cv-lator-backend-api",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker + Container proxy for the FastAPI backend (repo root: ../../backend).",
  "scripts": {
    "deploy": "wrangler deploy",
    "deploy:vars": "node scripts/deploy-with-worker-vars.mjs",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250321.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.78.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.2.0"
  }
}
