{
  "name": "cv-lator-pdf-service",
  "private": true,
  "version": "0.0.0",
  "description": "Cloudflare Worker + Container proxy for dedicated PDF generation service.",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "typecheck": "tsc --noEmit",
    "secrets:list": "wrangler secret list",
    "secrets:push": "node scripts/push-worker-secrets.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250321.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.79.0"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.2.0"
  }
}
