/**
 * Alternate marketing homepage (default hero copy + sections) at `/home`.
 */
import React from "react";
import { HomePage } from "./home/HomePage";

const HomeMarketing: React.FC = () => <HomePage />;

export default HomeMarketing;
